# ── DN42 Peering Request ──────────────────────────────────────
#  Fill in the fields below, then Ctrl+S save, Ctrl+X exit.
#  Exit WITHOUT saving to cancel. Empty optional fields are fine.
#
#  Peer port formula : 20000 + (__ASN__ % 10000) = __DEFAULT_PORT__ by default.
#  BGP Session Mode  : MP-BGP (Extended Next Hop)


# ── Target ────────────────────────────────────────────────────
Node                      = __NODE__
#
# ── Your addresses ────────────────────────────────────────────
Link-Local IPv6 (LLA)     = __LLA__
DN42 IPv4 (Optional)      = __IPV4__
IPv6 ULA (Optional)       = __ULA__
#
# ── Keys & endpoint ───────────────────────────────────────────
WireGuard Public Key      = __PUBKEY__
WireGuard Endpoint        = __ENDPOINT__
#
# ── Ports ─────────────────────────────────────────────────────
PeerPort                  = __PEER_PORT__
ListenPort                = __LISTEN_PORT__
#     (auto = convention port; PeerPort = 20000 + your ASN tail, ListenPort = 23143; auto-shift on conflict)
#
# ── Tunnel ────────────────────────────────────────────────────
MTU                       = __MTU__
