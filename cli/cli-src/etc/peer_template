# DN42 Peering Request — fill in, then Ctrl+O save, Ctrl+X exit
# Exit WITHOUT saving to cancel. Empty optional fields are fine.
# Peer port: 20000 + (__ASN__ % 10000) = __DEFAULT_PORT__ by default.
Node=__NODE__
Link-Local IPv6 (LLA)=__LLA__
DN42 IPv4 (Optional)=
IPv6 ULA (Optional)=
WireGuard Public Key=
WireGuard Endpoint=
PeerPort=auto
# assigned on submit — the port you dial (or custom: 20000-65535)
ListenPort=auto
# assigned on submit — your local listen port (or custom: 20000-65535)
MTU=1420
