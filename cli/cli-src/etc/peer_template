# ── DN42 Peering Request ──────────────────────────────────────
#  Fill in the fields below, then Ctrl+S save, Ctrl+X exit.
#  Exit WITHOUT saving to cancel. Empty optional fields are fine.
#
#  Peer port formula : 20000 + (__ASN__ % 10000) = __DEFAULT_PORT__ by default.
#  BGP Session Mode  : MP-BGP (Extended Next Hop)


# ── Target ────────────────────────────────────────────────────
Node                      = __NODE__
#
# ── Your addresses ────────────────────────────────────────────
Link-Local IPv6 (LLA)     = __LLA__
DN42 IPv4 (Optional)      =
IPv6 ULA (Optional)       =
#
# ── Keys & endpoint ───────────────────────────────────────────
WireGuard Public Key      =
WireGuard Endpoint        =
#
# ── Ports ─────────────────────────────────────────────────────
PeerPort                  = auto
ListenPort                = auto
#     (auto = assigned on submit; or custom 20000-65535)
#
# ── Tunnel ────────────────────────────────────────────────────
MTU                       = 1420
